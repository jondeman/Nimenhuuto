# .github/workflows/nimenhuuto.yml
name: Nimenhuuto-notifier

on:
  schedule:
    - cron: '0 5-21 * * *'      # käynnistyy tasatunnein 05–21 UTC
  workflow_dispatch:            # käsin käynnistettävä

jobs:
  run:
    runs-on: ubuntu-latest

    env:                         # salaisuudet ympäristömuuttujiksi
      NIMENHUUTO_USER:  ${{ secrets.NIMENHUUTO_USER }}
      NIMENHUUTO_PASS:  ${{ secrets.NIMENHUUTO_PASS }}
      TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      STATE_DIR:        ${{ github.workspace }}/state

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ensure state dir exists
        run: mkdir -p state

      - name: Run notifier
        run: python main.py

      # ----- Commit tilatiedostot + loki branchille -----
      - name: Commit state & log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state/events_seen.json state/app_run.log
          # Commitoi vain jos jotain muuttui
          if ! git diff --staged --quiet; then
            git commit -m "Update state & log $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
            git push
          fi
